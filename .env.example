# GitHub API Token (Required)
# Create a personal access token at: https://github.com/settings/tokens
# Required scopes: read:org, repo, user
GITHUB_TOKEN=ghp_your_token_here

# Lock to Specific GitHub User (Optional)
# When set, the API will only serve data for this user
# If a userName query parameter is provided, it will return an error
# If no userName is provided, it will use this value
# LOCK_GITHUB_USER=cyrus2281

# Cache Configuration (Optional)
# Maximum number of cached responses (default: 100)
# CACHE_MAX_SIZE=100

# Cache TTL in milliseconds (default: 3600000 = 1 hour)
# CACHE_TTL_MS=3600000